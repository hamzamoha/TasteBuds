{
  "scripts": {
    "dev": "npx tailwindcss -i tailwind.css -o ./public/style.css --watch",
    "build": "npx tailwindcss -i tailwind.css -o ./public/style.css --minify",
    "serve": "php -t public -S 0.0.0.0:8000"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  }
}
